<% if object.errors.any? %>
  <div id="validation_errors">
    <h3> <%= pluralize(object.errors.count, "error") %> prohibited from being saved:</h3>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li> <%= msg %> </li>
      <% end %>
    </ul>
  </div>
<% end %>

