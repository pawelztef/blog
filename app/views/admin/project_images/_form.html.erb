<%= form_for(@admin_project_image) do |f| %>
  <% if @admin_project_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_project_image.errors.count, "error") %> prohibited this admin_project_image from being saved:</h2>

      <ul>
      <% @admin_project_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
